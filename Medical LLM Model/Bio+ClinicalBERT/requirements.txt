# Bio+ClinicalBERT CLI + access checker requirements
# Install:
#   python -m pip install -r requirements.txt
# // AI-SUGGESTION: include transformers/torch for actual inference; keep the set minimal.

huggingface-hub>=0.22.0
transformers>=4.36.0
torch>=2.0.0
