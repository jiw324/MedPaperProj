# Meditron CLI requirements
# Install:
#   python -m pip install -r requirements.txt

transformers>=4.34.0
torch>=2.0.1
accelerate>=0.24.0
huggingface-hub>=0.19.0

# Optional (only needed if you use --quant 8bit/4bit)
bitsandbytes>=0.41.0


